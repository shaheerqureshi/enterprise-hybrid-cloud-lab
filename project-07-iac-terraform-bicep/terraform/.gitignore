# Terraform provider plugins — downloaded fresh with terraform init
.terraform/

# State files — contain sensitive infrastructure info
terraform.tfstate
terraform.tfstate.backup

# Lock file — optional to commit but we'll exclude for simplicity
.terraform.lock.hcl

# Variable files that might contain passwords
*.tfvars
*.tfvars.json

# Crash logs
crash.log